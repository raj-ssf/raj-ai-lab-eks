apiVersion: v1
kind: ServiceAccount
metadata:
  name: rag-service
  # namespace: set by overlay (`namespace:` field in kustomization.yaml)
  # annotations.eks.amazonaws.com/role-arn: injected by overlay patch
