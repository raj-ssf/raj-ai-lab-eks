apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Env-specific values (IRSA role ARN, ECR image URL, hostname) are injected by the
# ArgoCD Application spec in raj-ai-lab-eks-infra/argocd-apps.tf — not here — so
# this file stays committable without leaking account IDs or domains.

namespace: rag

resources:
  - ../../base
