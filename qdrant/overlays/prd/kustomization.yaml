apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qdrant

resources:
  - ../../base

# Add env-specific patches here when prod exists — multi-replica, API key auth,
# larger resource requests, anti-affinity across AZs, etc.
