apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qdrant

resources:
  - ../../base

# Add env-specific patches here when staging exists — larger PVC, more replicas,
# API key via Secret, etc.
